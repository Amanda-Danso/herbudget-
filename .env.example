# Backend environment variables — copy backend/.env.example to backend/.env
# and fill in real values there. This root-level file is just a pointer;
# see backend/.env.example for the authoritative template.
